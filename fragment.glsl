#version 300 es

precision highp float;

// uniform vec4 color;
// uniform sampler2D tex;
// uniform int mode;
// uniform float opacity;

// in vec2 uvs;

out vec4 FragColor;

void main() {
    FragColor = vec4(0.3, 0.5, 0.7, 1.0);
}